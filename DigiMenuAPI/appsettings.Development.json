{
  "ConnectionStrings": {
    "DefaultConnection": "Server=RONALD_PC;Database=DigiMenu;Integrated Security=False;User ID=sa;Password=Ronald01;TrustServerCertificate=True;Connect Timeout=30;Max Pool Size=100;Min Pool Size=5;Application Name=DigiMenuAPI;ConnectRetryCount=3;ConnectRetryInterval=5;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // ── Dev Tunnel (Visual Studio 2022) ────────────────────────────────────
  // Reemplaza TUNNEL-FRONTEND con la URL del túnel del frontend (VS Code).
  //
  // Cómo crear el túnel del BACKEND (Visual Studio 2022):
  //   Debug → Dev Tunnels → Create a Tunnel
  //   Nombre: DigiMenuAPI | Tipo: Persistent | Acceso: Public
  //   La URL se copia desde Debug → Dev Tunnels → (túnel) → Copy Tunnel URL
  //
  // Cómo obtener la URL del FRONTEND (VS Code):
  //   Panel "Ports" → puerto 4200 → columna "Forwarded Address"
  //
  // Formato de URL devtunnels:
  //   https://{usuario}-{tunnelname}-{puerto}.{región}.devtunnels.ms
  // ───────────────────────────────────────────────────────────────────────
  "AllowedOrigins": [
    "http://localhost:5173",
    "http://localhost:4200",
    "https://jf0w2wjh-4200.use.devtunnels.ms"
  ]
}
